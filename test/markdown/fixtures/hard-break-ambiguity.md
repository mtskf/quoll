<!-- case: hard break syntax — two-space line ending vs backslash line ending -->
Line one ends with two spaces.  
Line two follows the two-space break.

Line three ends with a backslash.\
Line four follows the backslash break.

A paragraph without any hard break
just a soft wrap inside one logical line.

Mixed inside one paragraph: first segment\
second segment after backslash break  
third segment after two-space break.
