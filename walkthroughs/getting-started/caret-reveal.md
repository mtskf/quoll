This is Quoll's signature trick: click into a **bold word**, a `# heading`, or a [link](https://example.com), and watch the raw Markdown syntax appear around your caret. Move away, and it conceals back into rendered text.

Nothing to click here — try it directly in your own document.
